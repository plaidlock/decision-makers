%h2 All Schools

%ul#actions
  %li= link_to '+ Add School', new_admin_school_path, :class => 'fancybox'

%table.striped.clickable{:width => '100%'}
  %tr
    %th Name
  - @schools.each do |school|
    %tr.clickable{:'data-href' => admin_school_path(school)}
      %td= school.name